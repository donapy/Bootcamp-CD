const removeButton = (element) => {
  element.style.display = "none";
};

const likeButton = () => {
  alert("ninja was liked");
};

const loginText = (element) => {
  element.textContent = "Logout";
};
